# Model / paper learning checklist

Status values: `scaffolded` (placeholders generated, not yet implemented),
`implemented` (placeholders filled in, runs end to end), `done` (implemented
+ notes/results captured in the model's README).

| # | Name | Type | Status | Path |
|---|------|------|--------|------|
| 01 | Linear regression | model | scaffolded | `models/01-linear-regression/` |

Add new rows as you decide what's next — via the `new-model` or
`paper-implementation` skills, or by hand.
